#include <iostream>

int main() {
    int num1 = 15;
    int num2 = 25;
    if (num1 > num2) {
        std::cout << num1 << " is the larger number." << std::endl;
    } else {
        std::cout << num2 << " is the larger number." << std::endl;
    }
    return 0;
}